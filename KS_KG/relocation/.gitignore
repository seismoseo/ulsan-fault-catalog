# generated artifacts (reproducible via run.sh / run_all.sh / build_*_nb.py)
# per-family dirs (catalog, members, reloc tables, thumbnails) — regenerable; aggregates force-added below
family*/
batch*.log
*.ipynb
__pycache__/
# committed aggregates are force-added: master_metrics.csv, batch_manifest.csv, master_map_relocated.png,
# and the flagship family738/ figures (reloc_*.csv, pygmt_reloc_*.png, *_seismicity.gif)
